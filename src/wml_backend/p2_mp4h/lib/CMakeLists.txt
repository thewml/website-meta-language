ADD_DEFINITIONS(-DHAVE_CONFIG_H)

INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_BINARY_DIR}/.. ${CMAKE_CURRENT_SOURCE_DIR}/..)
REPLACE_FUNCTIONS(LIB_SOURCES obstack xmalloc xstrdup)
SET(mp4h_STAT_SRCS
   error.c
   ${LIB_SOURCES}
   )

ADD_LIBRARY(mp4h_lib STATIC ${mp4h_STAT_SRCS})
